/lib/
/bin/
/.shards/
*.dwarf

# Libraries don't need dependency lock
# Dependencies will be locked in applications that use them
/shard.lock

# Crystal build cache (shared per AGENTS.md)
/.crystal-cache/

# Temporary files (per AGENTS.md)
/temp/
.claude/
