Version: "1.7.0-dev"


Excluded:
  - spec/**/*.cr
  - lib/**/*
  - temp/**/*
  - vendor
  - crystal
  - src/**/*.bak

# Add metric complexity file violations here
# Metrics/CyclomaticComplexity:
#   Excluded:
#     - src/bubbles/cursor.cr

# This project intentionally preserves upstream Go-like API names (`get_*`,
# non-`?` bool readers) for parity and compatibility.
Naming/AccessorMethodName:
  Enabled: false

Metrics/CyclomaticComplexity:
  Excluded:
    - src/huh/fields/confirm.cr
    - src/huh/fields/select.cr
    - src/huh/fields/multiselect.cr
